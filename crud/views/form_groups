<form method="POST" class="form">
    <div class="form-group">
        <label>Название группы *</label>
        <input type="text" name="group_name" value="<?= htmlspecialchars($data['group_name'] ?? $item['group_name'] ?? '') ?>" required>
        <?php if (isset($errors['group_name'])): ?>
            <span class="error"><?= $errors['group_name'] ?></span>
        <?php endif; ?>
    </div>
    
    <div class="form-group">
        <label>Вместимость</label>
        <input type="number" name="current_capacity" value="<?= htmlspecialchars($data['current_capacity'] ?? $item['current_capacity'] ?? '0') ?>">
    </div>
    
    <div class="form-buttons">
        <button type="submit" class="btn btn-primary"><?= $isEdit ? 'Сохранить' : 'Создать' ?></button>
        <a href="index.php?entity=sport_groups&action=list" class="btn">Отмена</a>
    </div>
</form>
